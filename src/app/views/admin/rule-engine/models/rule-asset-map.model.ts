/**
 *This model contain the key value pair for the payload and its associated asset
 *It maps the rule to a domain of asset.
 */
export class RuleAssetModel {
    assetId: string;
    assetSerialNumber: string;
    assetType: string;
}
