import { FullScreenIconDirective } from './full-screen-icon.directive';

describe('FullScreenIconDirective', () => {
  it('should create an instance', () => {
    const directive = new FullScreenIconDirective();
    expect(directive).toBeTruthy();
  });
});
