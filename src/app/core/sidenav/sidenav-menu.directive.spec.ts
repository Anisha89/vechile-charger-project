import { SidenavMenuDirective } from './sidenav-menu.directive';

describe('SidenavMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SidenavMenuDirective();
    expect(directive).toBeTruthy();
  });
});
